import spinner from '../../resources/img/Iphone-spinner-2.gif';

const Spinner = () => {
    return (
        <img src={spinner} alt='spinner' style={{ margin: '0 auto'}}/>
    )
}

export default Spinner;